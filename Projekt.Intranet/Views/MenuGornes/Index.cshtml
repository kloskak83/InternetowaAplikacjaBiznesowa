@model IEnumerable<BazaDanych.Data.Menu.MenuGorne>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NaglowekIntranet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NazwaPortal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypPolaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NaglowekIntranet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NazwaPortal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypPolaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMenuGorne">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMenuGorne">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMenuGorne">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
